<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualizar Película</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="../public/css/bootstrap.min.css">
    <link rel="stylesheet" href="../public/css/menu.css">
    <link rel="stylesheet" href="../public/css/home.css">
    <!-- Incluir el archivo head.ejs -->
    <%- include('../includes/head') %>
    <style>
        /* Ajustes de estilo */
        form {
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
        }
        .container {
            margin-top: 80px !important;
        }
        .form-label {
            font-weight: bold;
        }
    </style>
</head>

<body>
    <header class="header">
        <input class="menu-btn" type="checkbox" id="menu-btn" />
        <label class="menu-icon" for="menu-btn"><span class="navicon"></span></label>
        <ul class="menu">
            <li>
                <a class="menu-item" href="/Crud-Completo-con-NodeJS-Express-y-MySQL">Inicio</a>
            </li>
            <li>
                <a class="menu-item" href="/salas">Salas</a>
            </li>
            <li>
                <a class="menu-item" href="/Crud-usuarios">Lista Usuarios</a>
            </li>
            <li>
                <a href="/lista-Peliculas">Peliculas</a>
            </li>
        </ul>
        <ul>
            <li class="menu-item-right">
                <a class="menu-item" href="/">Cerrar sesión</a>
            </li>
        </ul>
    </header>

    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col-md-5">
                <h3 class="text-center">Actualizar Película</h3>
                <div id="actualizarPeliculaForm">
                    <form id="formActualizarPelicula" action="/actualizar-pelicula/<%= pelicula.id_pelicula %>" method="POST" enctype="multipart/form-data" onsubmit="return validarActualizarPelicula()">
                        <input type="hidden" name="_method" value="PUT"> 
                    
                        <div class="mb-3">
                            <label for="nombre_pelicula" class="form-label">Nombre Película</label>
                            <input type="text" name="nombre_pelicula" id="nombre_pelicula" class="form-control" value="<%= pelicula.nombre_pelicula %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="sinopsis" class="form-label">Sinopsis</label>
                            <input type="text" name="sinopsis" id="sinopsis" class="form-control" value="<%= pelicula.sinopsis %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="duracion" class="form-label">Duración</label>
                            <input type="text" name="duracion" id="duracion" class="form-control" value="<%= pelicula.duracion %>" required>
                        </div>
                        <div class="mb-3">
                            <label for="genero" class="form-label">Género</label>
                            <select class="form-select" name="genero" id="genero" required>
                                <option value="">Seleccionar Género</option>
                                <option value="Acción" <%= pelicula.genero === 'Acción' ? 'selected' : '' %>>Acción</option>
                                <option value="Comedia" <%= pelicula.genero === 'Comedia' ? 'selected' : '' %>>Comedia</option>
                                <option value="Drama" <%= pelicula.genero === 'Drama' ? 'selected' : '' %>>Drama</option>
                                <!-- Agrega más opciones según sea necesario -->
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="estado" class="form-label">Estado de Película</label>
                            <select class="form-select" name="estado" id="estado" required>
                                <option value="">Seleccionar Estado de Película</option>
                                <option value="Activa" <%= pelicula.estado === 'Activa' ? 'selected' : '' %>>Activa</option>
                                <option value="Inactiva" <%= pelicula.estado === 'Inactiva' ? 'selected' : '' %>>Inactiva</option>
                                <!-- Agrega más opciones según sea necesario -->
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="imagen" class="form-label">Imagen</label>
                            <input type='file' name="imagen" id="imagen" class="form-control">
                        </div>
                        <div class="mb-3">
                            <label for="precio" class="form-label">Precio</label>
                            <input type="text" name="precio" id="precio" class="form-control" value="<%= pelicula.precio %>" required>
                        </div>
                        <div class="row">
                            <div class="col">
                                <div class="d-grid gap-2 col-12 mx-auto ">
                                    <button class="btn btn-primary btn-block" type="submit">Actualizar Película</button>
                                </div>
                            </div>
                            <div class="d-grid gap-2 col-12 mx-auto mt-4 mb-4">
                                <a class="btn btn-primary btn-block" href="/lista-Peliculas">Cancelar</a>
                            </div>
                        </div>
                    </form>
                    
                </div>
            </div>
        </div>
    </div>

    <!-- Incluir archivos de script -->
    <%- include('../includes/scripts') %>
</body>

<style>
    form {
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
      }
</style>
</html>
